- location_picture_xrefs.each do |lpx|
  - if (lpx.photo? && lpx.approved?)
    = link_to image_tag(lpx.url(:thumb)), lpx.url(:medium), :class => "location_picture_xref_#{lpx.id} highslide", :onclick => 'return hs.expand(this);'
