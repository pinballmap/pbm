- LocationPictureXref.find_all_by_location_id(location.id).each do |lpx|
  - if (lpx.photo? && lpx.approved?)
    = link_to image_tag(lpx.photo_url(:thumb)), lpx.photo_url, :class => "location_picture_xref_#{lpx.id} highslide", :onclick => 'return hs.expand(this);'
