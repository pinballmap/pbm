#page_body
  .left_column.secondary.profile
    %p.donate.darkb #{@user.username}
    %p.darkbl{:style => "font-size:16px;"} Member since: #{@user.created_at.strftime('%b-%d-%Y')}
    - if !Authorization.current_user.is_a?(Authorization::AnonymousUser) && (@user.id == Authorization.current_user.id)
      %p
        =link_to "Update email or password", "http://#{request.host_with_port}/users/edit"
  .right_column.right_column_profile
    %div.darkbl.stats
      %div.stat_num_o
        %span.stats
          #{@user.num_machines_added}
      %span.stats_text Machines Added
    %div.darkbl.stats
      %div.stat_num_o
        %span.stats
          #{@user.num_machines_removed}
      %span.stats_text Machines Removed
    %div.darkbl.stats
      %div.stat_num_o
        %span.stats
          #{@user.num_lmx_comments_left}
      %span.stats_text Conditions Left
    %div.darkbl.stats
      %div.stat_num_o
        %span.stats
          #{@user.num_locations_suggested}
      %span.stats_text Locations Suggested
    %div.darkbl.stats
      %div.stat_num_o
        %span.stats
          #{@user.num_locations_edited}
      %span.stats_text Locations Edited
    %div.darkbl.stats
      %div.stats_o
        List of Locations Edited:
        %span.stats_list
          - @user.profile_list_of_edited_locations.each do |location_array|
            - location = Location.find(location_array[0])
            %span.location_edited
              <a href="http://#{request.host_with_port}/#{location.region.name}/?by_location_id=#{location.id}">#{location.name}</a>
            %br/
    %div.darkbl.stats
      %div.stats_o
        High Scores:
        %span.stats_list
          - @user.profile_list_of_high_scores.each do |score_array|
            - location_name, machine_name, score, date_created = score_array
            %span.score_machine
              #{machine_name}
            %span.score_score
              #{number_with_precision(score, precision: 0, delimiter: ',')}
            %span.score_meta at
            %span.score_meta_gen #{location_name}
            %span.score_meta on
            %span.score_meta_gen #{date_created}
