<!DOCTYPE html>
<html>
<head>
  <title>Pinball Map</title>
  <%= stylesheet_link_tag 'style', 'jquery.ui.all' %>
  <%= javascript_include_tag 'jquery', 'jquery-ui-1.8.5.custom.min', 'rails', 'application' %>
  <%= csrf_meta_tag %>
  <script type="text/javascript"
      src="http://maps.google.com/maps/api/js?sensor=true">
  </script>
</head>
<body onload="initializeMap()">
<div id="user_nav">
  <% if user_signed_in? %>
    Signed in as <%= current_user.email %>
    <%= link_to 'Log out', destroy_user_session_path %>
  <% else %>
    <%= link_to 'Sign up', new_user_registration_path %>
    <%= link_to 'Log in', new_user_session_path %>
  <% end %>
</div>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

<%= render :partial => 'locations/form' %>
<div id="locations"><%= render 'locations/locations' %></div>
<%= render :partial => 'locations/map' %>

</body>
</html>
