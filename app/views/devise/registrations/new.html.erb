<div class="page_login">
	<div class="column_container">
		<div class="column w_320 bg_gray">
			<h2>
				Why join?
			</h2>
			<p>
				Because updating pinball maps is fun! Also, it's useful to others and it's maintained entirely by people like you! Signing up is simple, and will allow you to add and remove machines from places, add comments about machines, etc. Plus you can track your contributions.
			</p>
			<p>
				We won't share your email address with anyone! View <%= link_to 'Privacy Policy', privacy_path %>.
			</p>
		</div>
		<div id="login" class="column w_320">
			<h2>Join Pinball Map</h2>

			<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
				<%= render "devise/shared/error_messages", resource: resource %>

				<div class="field form-inputs">
					<p><%= f.label :username %></p>
					<p><%= f.text_field :username, autofocus: true, required: true %></p>
				</div>

				<div class="field form-inputs">
					<p><%= f.label :email %></p>
					<p><%= f.email_field :email, autocomplete: "email", required: true %></p>
					<span class="font14 pt_5 grey block">(Disposable emails are not allowed and you will get banned)</span>
				</div>

				<div class="field form-inputs">
					<p><%= f.label :password %></p>
					<% if @minimum_password_length %>
					<p><em>(<%= @minimum_password_length %> characters minimum)</em></p>
					<% end %>
					<p><%= f.password_field :password, autocomplete: "new-password", required: true %></p>
					<span class="font14 pt_5 grey block">(Choose a password that you don't use elsewhere)</span>
				</div>

				<div class="field form-inputs">
					<p><%= f.label :password_confirmation %></p>
					<p><%= f.password_field :password_confirmation, autocomplete: "new-password", required: true %></p>
				</div>

				<div class="field form-inputs">
					<p class="red bold">Security Question (required):</p>
					<%= label_tag :security_question, "What are we mapping on this site?" %><br />
					<%= text_field_tag :security_question, nil, autocomplete: "off", required: true %>
				</div>

				<div class="actions">
					<%= f.submit "Sign up", class: "button" %>
				</div>
				<br />
			<% end %>

			<div style="margin-top: 10px;">If you don't receive the email, whitelist admin@pinballmap.com and try again, and <span class="bold brightpurple">check your spam folder</span>.</div>

			<%= render "devise/shared/links" %>
		</div>
	</div>
</div>
