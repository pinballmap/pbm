!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %p
      100% of pinball machines have an issue at some point (and everyone knows this). But not all machines get fixed. Machine comments listing problems are GOOD, because they are an opportunity for you to demonstrate that yours are maintained (e.g., "issue fixed!").
    %p
      Here are the edits made to your pinball machines yesterday on Pinball Map. We hope this helps you identify and fix problems.
    %p
      You opted in to receive these emails, but if you don't want them anymore, reply to this message and tell us. Also, see our FAQ:
      %a{href: "https://pinballmap.com/faq#operators"}= "https://pinballmap.com/faq#operators"
    %h3
      Machine Comments
    - @machine_comments.each do |item|
      %p
        %a{href: "https://pinballmap.com/map?by_location_id=#{item[:location_id]}"}= item[:location_name]
        %br/
        = item[:machine_name]
        %br/
        "#{item[:comment]}"
        %br/
        = item[:user_name]
    %h3
      Machines Added
    - @machines_added.each do |item|
      %p
        %a{href: "https://pinballmap.com/map?by_location_id=#{item[:location_id]}"}= item[:location_name]
        %br/
        = item[:machine_name]
        %br/
        = item[:user_name]
    %h3
      Machines Removed
    - @machines_removed.each do |item|
      %p
        %a{href: "https://pinballmap.com/map?by_location_id=#{item[:location_id]}"}= item[:location_name]
        %br/
        = item[:machine_name]
        %br/
        = item[:user_name]
