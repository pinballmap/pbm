%div.bold.font20.center.p_10.brightpurple
  High Scores
%div[lmx, :score_container]
  %div[lmx, :show_high_scores]
    = render :partial => 'location_machine_xrefs/render_machine_scores', :locals => {:scores => lmx.sorted_machine_scores(current_user&.id), :highest => lmx.highest_machine_score(current_user), all_scores: lmx.sorted_machine_all_scores, :lmx => lmx}
