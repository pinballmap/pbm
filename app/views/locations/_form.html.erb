<%= form_tag locations_path, :method => 'get', :id => 'location_search' do %>
  <%= label_tag :by_name, 'Name' %>
  <%= text_field_tag :by_name, params[:by_name] %>
  <%= submit_tag 'Search', :name => nil %>
  <div id="locations"><%= render 'locations/locations' %></div>
<% end %>
