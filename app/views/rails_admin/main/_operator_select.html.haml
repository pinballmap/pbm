%select{:name => "#{object_type}[operator_id]"}
  <option value=""></option>
  - Operator.all.select {|o| o.region_id == Authorization.current_user.region_id}.each do |o|
    <option value="#{o.id}" #{o.id == operator_id ? 'selected=select' : ''} >#{o.name}</option>
